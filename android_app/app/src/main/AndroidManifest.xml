<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Mikrofon-Berechtigung für Audio-Aufnahme -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Vibration für Feedback (Phase 1) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Optional: Speicher-Zugriff falls Test-Dateien geladen werden -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Audio_digit_app">

        <!-- NEUE MainActivity mit Raw Waveform Pipeline -->
        <activity
            android:name="com.orez74.speechdigits.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dataset Recorder für Trainingsdaten -->
        <activity
            android:name="com.orez74.speechdigits.DigitDatasetRecorderActivity"
            android:exported="false"
            android:label="Dataset Recorder" />

        <!-- Settings (Phase 1) -->
        <activity
            android:name="com.orez74.speechdigits.SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:parentActivityName="com.orez74.speechdigits.MainActivity" />

    </application>

</manifest>
