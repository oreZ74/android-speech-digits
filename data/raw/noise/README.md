# Noise

Background noise files used for data augmentation during training. Not committed to this repository.

The pipeline uses `data/raw/speech_commands/_background_noise_/` by default. Place any additional WAV files here if needed.

Format: WAV, 16-bit, 16 kHz (any duration — randomly cropped during augmentation).
